<script>
  // Ensure animations trigger when the section is visible
  document.addEventListener("DOMContentLoaded", function () {
    const heroContent = document.querySelector(".hero-content");
    heroContent.style.opacity = "1";
    heroContent.style.transform = "translateY(0)";
  });
</script>

<div class="hero-section">
  <video autoplay loop muted playsinline class="hero-background-video">
    <source src="{{ section.settings.background_video_url }}" type="video/mp4">
  </video>
  <div class="hero-content">
    <h1>{{ section.settings.hero_heading }}</h1>
    <p>{{ section.settings.hero_text }}</p>
  </div>
</div>
<div class="marquee-wrapper">
   <div class="marquee">
      <span>{{ section.settings.marquee_slot_1 }}</span>
      <span>{{ section.settings.marquee_slot_2 }}</span>
      <span>{{ section.settings.marquee_slot_3 }}</span>
      <span>{{ section.settings.marquee_slot_4 }}</span>
      <span>{{ section.settings.marquee_slot_5 }}</span>
      <span>{{ section.settings.marquee_slot_6 }}</span>
      <span>{{ section.settings.marquee_slot_7 }}</span>
      <span>{{ section.settings.marquee_slot_8 }}</span>
       <span>{{ section.settings.marquee_slot_1 }}</span>
      <span>{{ section.settings.marquee_slot_2 }}</span>
      <span>{{ section.settings.marquee_slot_3 }}</span>
      <span>{{ section.settings.marquee_slot_4 }}</span>
      <span>{{ section.settings.marquee_slot_5 }}</span>
      <span>{{ section.settings.marquee_slot_6 }}</span>
      <span>{{ section.settings.marquee_slot_7 }}</span>
      <span>{{ section.settings.marquee_slot_8 }}</span>
         <span>{{ section.settings.marquee_slot_1 }}</span>
      <span>{{ section.settings.marquee_slot_2 }}</span>
      <span>{{ section.settings.marquee_slot_3 }}</span>
      <span>{{ section.settings.marquee_slot_4 }}</span>
      <span>{{ section.settings.marquee_slot_5 }}</span>
      <span>{{ section.settings.marquee_slot_6 }}</span>
      <span>{{ section.settings.marquee_slot_7 }}</span>
      <span>{{ section.settings.marquee_slot_8 }}</span>
         <span>{{ section.settings.marquee_slot_1 }}</span>
      <span>{{ section.settings.marquee_slot_2 }}</span>
      <span>{{ section.settings.marquee_slot_3 }}</span>
      <span>{{ section.settings.marquee_slot_4 }}</span>
      <span>{{ section.settings.marquee_slot_5 }}</span>
      <span>{{ section.settings.marquee_slot_6 }}</span>
      <span>{{ section.settings.marquee_slot_7 }}</span>
      <span>{{ section.settings.marquee_slot_8 }}</span>
         <span>{{ section.settings.marquee_slot_1 }}</span>
      <span>{{ section.settings.marquee_slot_2 }}</span>
      <span>{{ section.settings.marquee_slot_3 }}</span>
      <span>{{ section.settings.marquee_slot_4 }}</span>
      <span>{{ section.settings.marquee_slot_5 }}</span>
      <span>{{ section.settings.marquee_slot_6 }}</span>
      <span>{{ section.settings.marquee_slot_7 }}</span>
      <span>{{ section.settings.marquee_slot_8 }}</span>
    </div>
</div>

<style>
.hero-section {
  position: relative;
  width: 100%;
  padding: 60px;
  height: 70vh;
  display: flex;
  align-items: end;
  justify-content: start;
  text-align: start;
  color: white;
  overflow: hidden;
}

.hero-background-video {
  border-radius: 10px;
  margin: 14px;
  margin-top: 0;
  position: absolute;
  top: 0;
  left: 0;
  width: calc(100% - 24px);
  height: 100%;
  object-fit: cover;
  z-index: -1;
  animation: zoom-in 10s ease-in-out infinite alternate;
}

@keyframes zoom-in {
  from {
    transform: scale(1);
  }
  to {
    transform: scale(1.1);
  }
}

.hero-content {
  opacity: 0;
  transform: translateY(20px);
  transition: all 1s ease-out;
}

.hero-content h1 {
  font-size: 3rem;
  font-weight: 900;
  margin-bottom: 0.5rem;
}

.hero-content p {
  font-size: 1rem;
}

.marquee-wrapper {
  padding: 10px 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 100%;
  overflow: hidden;
}

.marquee {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10vw;
  white-space: nowrap;
  animation: marquee-scroll 28s linear infinite;
}

.marquee span {
  font-size: 0.75rem;
  color: black;
  text-transform: uppercase;
  font-weight: 900;
  animation: fade-in 1s ease-in-out;
}

.marquee span:hover {
  color: #ff6347; /* Adds a hover effect for modern styling */
  font-size: 0.9rem;
}

@keyframes marquee-scroll {
  from {
    transform: translateX(0%);
  }
  to {
    transform: translateX(-50%);
  }
}

@media (max-width: 768px) {
  .hero-content h1 {
    font-size: 2rem;
  }
  .hero-content p {
    font-size: 1rem;
  }
}

@keyframes fade-in {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}
</style>
