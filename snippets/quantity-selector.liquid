<div class="flex items-center space-x-2">
  <button 
    type="button" 
    class="bg-black text-white rounded-full w-8 h-8 flex items-center justify-center font-bold"
    onclick="this.nextElementSibling.stepDown(); this.nextElementSibling.dispatchEvent(new Event('change'));">
    &minus;
  </button>
  <input
    class="input-number w-12 text-center border border-gray-300 rounded-md" 
    name="{{ name | default: 'quantity' }}" 
    value="{{ value | default: 1 }}" 
    min="{{ min | default: 1 }}" 
    type="number" 
    required 
    {% if readonly %}readonly{% endif %}
  />
  <button 
    type="button" 
    class="bg-black text-white rounded-full w-8 h-8 flex items-center justify-center font-bold"
    onclick="this.previousElementSibling.stepUp(); this.previousElementSibling.dispatchEvent(new Event('change'));">
    &plus;
  </button>
</div>
